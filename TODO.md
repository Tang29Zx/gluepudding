# 3D 实时站点大世界待办清单

来源：用户当前设想与 `fortune_world_complete_plan.md`

本文档只记录我需要推进的前端大世界和集成任务。后端、建模、RDK、WebRTC 推流服务和硬件控制服务是外部能力来源，不作为我的实现任务。

## P0. 重新确定项目落点

- [x] 确认 `gluepudding` 是 3D 大世界入口。
- [x] 确认 `iot`、`game`、`auth` 保持独立项目边界。
- [x] 确认核心交互不做整页跳转。
- [x] 确认第一版源码目录为 `gluepudding/app/nav-world/`。
- [x] 确认构建输出目录为 `gluepudding/app/frontend/`。
- [x] 保留现有静态导航页内容作为 WebGL 失败兜底或 2D 菜单来源。
- [x] 根据锁文件确认包管理器（`gluepudding` 当前无前端 lock file，Layer 0 不使用包管理器）。
- [x] 建立 Vite React TypeScript 项目。
- [x] 安装或确认 `three`、`@react-three/fiber`、`@react-three/drei`（`zustand` 延后到 Layer 3 状态共享需求明确后再引入）。

## P1. 3D 世界骨架

- [x] 创建 3D 世界入口页面。
- [x] 创建 `WorldScene.tsx`。
- [x] 创建地面、天空、基础灯光和相机。
- [x] 创建玩家占位体。
- [x] 实现 `WASD` 移动。
- [x] 实现 `Shift` 疾跑。
- [x] 实现 `Space` 跳跃。
- [x] 实现鼠标视角或朝向控制。
- [x] 实现 `ESC` 关闭当前面板。
- [x] 建立中心出生点。
- [x] 创建占卜屋占位区域。
- [x] 创建实验室占位区域。
- [x] 创建草地棋盘占位区域。
- [x] 没有任何外部服务时，世界仍然可以渲染和移动。

## P2. 世界内交互系统

- [x] 创建 `InteractionSystem.tsx`。
- [x] 定义可交互对象数据结构。
- [x] 支持距离检测。
- [x] 支持鼠标射线检测。
- [x] 支持对象悬停高亮。
- [x] 支持 `E` 区域级交互。
- [x] 支持准星命中后的左键对象级交互。
- [x] 支持当前选中对象状态。
- [x] 支持世界内提示文案。
- [x] 支持面板打开时暂停移动。
- [x] 支持面板关闭后恢复移动。
- [x] 移动端补充点击或按钮替代左键对象选择。

## P3. 世界模块系统

- [x] 创建模块注册表。
- [x] 创建占卜屋模块 `modules/divination/`。
- [x] 创建实验室模块 `modules/laboratory/`。
- [x] 创建五子棋模块 `modules/gomoku/`。
- [x] 创建常驻 3D 世界内模块表面组件。
- [x] 创建共享加载中、错误、离线状态组件。
- [x] 用准星射线和 Canvas 左键 / `E` 操作模块状态控件。
- [ ] 后续优化：进入某个区域才加载对应模块，其余模块不加载。
- [x] 确保模块错误不会导致整个世界崩溃。

## P3.5. 资源接入准备

- [x] 记录世界大场景候选资源：`resources/float-island-low-ploy.zip`。
- [x] 记录占卜屋模型资源输入：`resources/fortune.zip` 和 `resources/fortune/`。
- [x] 记录占卜屋现有功能实现代码输入：`resources/feature-implementation/`。
- [x] 明确 `resources/feature-implementation/node_modules/`、`resources/feature-implementation/dist/` 和 `resources/feature-implementation/.env` 不作为主工程源码接入输入。
- [x] 检查并解压大场景资源，确认模型格式、比例、贴图和许可备注。
- [x] 解压占卜屋模型资源并确认可用 GLB 清单。
- [x] 规划大场景资源迁移目标目录，例如 `gluepudding/app/nav-world/public/models/world/`。
- [x] 清理大场景 GLB 上的动物节点，并保留原始 zip 备份。
- [x] 将浮岛 GLB 作为世界基础地形接入，旧圆形地板只作为加载失败兜底。
- [x] 基于 GLB 主岛体最高朝上命中面实现玩家真实贴地和岛边限制。
- [x] 将出生点、占卜屋、实验室和五子棋区域重排到浮岛空地。
- [ ] 梳理 `resources/feature-implementation/src/` 中可复用的类型、API 适配、mock 数据和 UI 流程。
- [ ] 将占卜屋接入任务拆到 Layer 5，不在资源准备层实现真实业务。

## P4. 占卜屋

Layer 5 已收口为占卜屋模型摆放验收：只确认帐篷、桌面、星座、塔罗、周易模型和空白内容屏位置。所有占卜屋交互逻辑，包括点击、选中、高亮、卡牌翻面、输入、结果展示、mock 流程和真实接口接入，统一下放到 `VALIDATION_LAYERS.md` 的 Layer 8。

- [x] 创建占卜屋模型外壳切片：帐篷外壳靠近后加载，屋内道具进入/聚焦后再加载。
- [x] 接入轻量占卜屋 GLB 资源，不复制完整塔罗牌面贴图目录。
- [x] 调整占卜屋模型摆位：帐篷门朝出生点，魔法阵作为平整基底，桌面、烛台、星座穹顶和周易道具重新摆放。
- [x] 创建星座台模型摆放。
- [x] 创建塔罗桌模型摆放。
- [x] 创建周易桌模型摆放。
- [x] 创建三块空白内容屏作为后续交互内容承载面。
- [ ] Layer 8：创建占卜屋真实场景区域交互。
- [ ] 创建 `fortuneTypes.ts`。
- [ ] 创建 `fortuneApi.ts`。
- [ ] 支持 `VITE_USE_MOCK` 或等价模拟数据开关。
- [ ] 添加星座模拟结果。
- [ ] 添加塔罗模拟结果。
- [ ] 添加周易模拟结果。
- [ ] 实现星座面板。
- [ ] 实现塔罗 3D 卡牌。
- [ ] 实现准星命中后左键选中塔罗牌。
- [ ] 实现选中卡牌高亮。
- [ ] 实现单张牌模式。
- [ ] 实现三张牌模式。
- [ ] 实现卡牌翻面。
- [ ] 实现塔罗结果展示。
- [ ] 实现周易铜钱动画占位。
- [ ] 使用 `result.lines.slice().reverse()` 展示六爻。
- [ ] 外部占卜接口不可用时，模拟数据仍然能跑完整流程。

## P5. 实验室基础场景

- [ ] 创建实验室房间或占位空间。
- [ ] 创建实验室大屏模型。
- [ ] 创建 RDK 展示台。
- [ ] 创建门禁控制台。
- [ ] 创建设备状态墙。
- [ ] 支持走进实验室后显示实验室提示。
- [ ] 支持准星命中后左键点击大屏进入放大查看。
- [ ] 支持准星命中后左键点击门禁控制台打开控制面板。
- [ ] 支持准星命中后左键点击 RDK 展示台查看模型状态。

## P6. WebRTC 大屏

- [ ] 创建 `webrtcClient.ts` 适配层。
- [ ] 定义 WebRTC 配置来源，不硬编码凭据。
- [ ] 支持连接中状态。
- [ ] 支持在线状态。
- [ ] 支持断线状态。
- [ ] 支持无权限状态。
- [ ] 将 `MediaStream` 绑定到隐藏 `video` 元素。
- [ ] 将 `video` 转成 Three.js 视频纹理。
- [ ] 把视频纹理贴到实验室大屏。
- [ ] 推流不可用时显示离线画面。
- [ ] 提供模拟视频或占位动态画面，保证演示不中断。

## P7. RDK 世界模型展示

- [ ] 创建 `rdkWorldClient.ts` 适配层。
- [ ] 定义 RDK 模型快照数据结构。
- [ ] 定义 RDK 模型实时更新订阅接口。
- [ ] 第一版支持加载静态 GLB 或模拟模型。
- [ ] 创建 RDK 展示台渲染组件。
- [ ] 支持模型旋转。
- [ ] 支持模型缩放。
- [ ] 支持节点选中。
- [ ] 支持显示节点状态。
- [ ] 支持更新时间展示。
- [ ] 实时数据不可用时显示最近一次快照或占位模型。
- [ ] 避免把 RDK 原始数据格式写死在渲染组件里。

## P8. 门禁控制台

- [ ] 创建 `iotApi.ts` 适配层。
- [ ] 封装 `getDoorState()`。
- [ ] 封装 `openDoor()`。
- [ ] 封装 `getDeviceStatus()`。
- [ ] 创建门禁控制台面板。
- [ ] 显示门禁当前状态。
- [ ] 显示设备在线状态。
- [ ] 开门前要求用户确认。
- [ ] 发送开门指令。
- [ ] 显示成功反馈。
- [ ] 显示失败反馈。
- [ ] 显示无权限反馈。
- [ ] 显示设备离线反馈。
- [ ] 没有真实 IoT 接口时，使用模拟状态演示。
- [ ] 不在前端保存任何 token、密码、私钥或设备密钥。

## P9. 世界内五子棋

- [ ] 创建草地棋盘区域。
- [ ] 创建五子棋打开交互。
- [ ] 第一阶段使用世界内面板承载五子棋。
- [ ] 评估现有 `game/app/frontend/wuziqi/` 是否可同源嵌入。
- [ ] 如果不能直接嵌入，规划同源代理或逻辑抽离。
- [ ] 处理棋局面板打开后与大世界键盘控制的冲突。
- [ ] 面板关闭后恢复大世界控制。
- [ ] 保留棋局状态或明确重置策略。
- [ ] 第二阶段设计原生 3D 棋盘。
- [ ] 第二阶段支持左键或点击棋盘交叉点落子。
- [ ] 第二阶段复用现有五子棋规则和 AI 逻辑。

## P10. 认证和权限

- [ ] 读取现有 `auth` 登录态接口能力。
- [ ] 世界内显示当前登录状态。
- [ ] 对门禁控制检查权限状态。
- [ ] 无权限时在世界内显示提示，不默认整页跳转。
- [ ] 登录流程如果当前系统必须跳转，记录为短期限制。
- [ ] 后续规划世界内登录弹窗或同源认证入口。
- [ ] 所有敏感错误信息只显示安全摘要。

## P11. 兜底和降级

- [x] WebGL 不可用时显示 2D 兜底菜单（Layer 0 静态页已迁入 React fallback，Layer 1 已接入 3D 初始化失败自动回落）。
- [ ] WebRTC 不可用时显示离线大屏。
- [ ] RDK 数据不可用时显示占位模型。
- [ ] IoT 接口不可用时显示模拟控制台或离线状态。
- [ ] 占卜接口不可用时使用模拟数据。
- [ ] 五子棋嵌入不可用时显示世界内错误面板。
- [ ] 低性能设备降低阴影、后处理和模型细节。
- [ ] 移动端提供触屏移动和对象选择。

## P12. MVP 验证清单

- [ ] 打开 `gluepudding` 首页后看到 3D 大世界。
- [ ] 不进入其他整页站点也能完成核心演示。
- [ ] 玩家可以移动。
- [ ] 可以进入占卜屋区域。
- [ ] 可以准星命中后左键选择塔罗牌。
- [ ] 塔罗结果能在世界内显示。
- [ ] 可以进入实验室区域。
- [ ] 实验室大屏能显示模拟视频或真实 WebRTC 画面。
- [ ] RDK 展示台能显示静态或模拟模型。
- [ ] 门禁控制台能显示状态并走模拟或真实开门流程。
- [ ] 可以进入草地棋盘区域。
- [ ] 五子棋能在世界内打开。
- [ ] 外部服务关闭时，大世界仍然可用。
- [ ] 运行类型检查。
- [ ] 运行静态检查或格式检查，如果项目提供对应命令。
- [ ] 做桌面端截图验证。
- [ ] 做移动端布局或替代交互验证。

## P13. 分阶段执行建议

### 第一阶段：3D Hub 骨架

- [x] 建立 `gluepudding/app/nav-world/`。
- [x] 搭建基础 3D 世界。
- [x] 实现移动、相机和交互系统。
- [x] 放置占卜屋、实验室、棋盘三个区域。

### 第二阶段：占卜屋 MVP

- [ ] 实现塔罗桌和左键选牌。
- [ ] 接入模拟数据。
- [ ] 实现结果展示。
- [ ] 保留星座和周易基础面板。

### 第三阶段：实验室 MVP

- [ ] 实现大屏视频纹理。
- [ ] 实现 RDK 模型展示台。
- [ ] 实现门禁控制台模拟流程。
- [ ] 接入真实能力时走适配层。

### 第四阶段：世界内五子棋

- [ ] 先实现世界内棋局面板。
- [ ] 解决同源嵌入或逻辑抽离问题。
- [ ] 后续再做原生 3D 棋盘。

### 第五阶段：真实能力接入和打磨

- [ ] 接真实 WebRTC 推流。
- [ ] 接真实 RDK 模型更新。
- [ ] 接真实门禁控制。
- [ ] 接真实占卜接口。
- [ ] 优化性能、移动端和视觉表现。


提醒自己最后使用的免费3d模型别忘了声明版权