# 2026-07-08 工作日志

## 环境配置：Node.js/npm 加入用户 PATH
- 用户在 PowerShell 运行 `npm run dev` 报 "无法将 npm 项识别为 cmdlet"。
- 原因：WorkBuddy 托管的 Node.js（`C:\Users\user\.workbuddy\binaries\node\versions\22.22.2`，含 node.exe / npm.cmd）未加入用户级 PATH，仅在 WorkBuddy bash 会话可用。
- 解决：用 `[Environment]::SetEnvironmentVariable("Path", ..., "User")` 将该目录追加到用户 PATH 末尾（用户级，无需管理员权限）。
- 注意：环境变量修改对已打开的 PowerShell 窗口不生效，需重开窗口。

## 依赖安装
- 按 README 在 `app/nav-world` 执行 `npm install`，成功（added 2 packages, changed 9 packages）。

## Dev server 启动
- 在 `app/nav-world` 执行 `npm run dev`，Vite v8.1.3 启动成功。
- 本地访问：http://localhost:5173/

## Layer 8 数据层与逻辑层迁移
- 从小学期作业 `resources/feature_implementation/` 迁移到 `app/nav-world/src/modules/divination/`：
  - `types.ts`：完整类型定义（12星座、78张塔罗牌、64卦、API 请求/响应类型），100% 直接复用。
  - `data/`：4 个 JSON 数据集（tarot_cards.json, tarot_cards_78.json, zodiac_texts.json, iching_64.json），作为 mock 数据源。
  - `fortuneApi.ts`：API 适配层，默认 mock 模式（VITE_USE_MOCK !== false），6 个接口函数。
  - `business/tarotLogic.ts`：卡牌标签函数 getCardLabel()、选牌校验。
  - `business/ichingLogic.ts`：铜钱法模拟 tossCoins()、64卦查表 lookUpHexagram()、变卦计算、完整起卦 simulateIchingCast()。
  - `business/zodiacLogic.ts`：日期到星座映射 getSignFromBirthday()。
- `npm run build` 通过，tsc 类型检查无错误，Vite 构建成功（43秒）。

## Layer 8 — 星座轮盘 3D 交互组件
- 新建 `ZodiacWheel.tsx`（~380 行），实现：
  - 12 个可点击星座球体环绕在 zodiac-wheel 模型上方（半径 1.72m，高度 y=1.05）。
  - 使用 useFrame + Raycaster 从相机中心射线检测命中，命中时球体高亮放大 + Billboard 文字放大。
  - 左键点击命中球体 → 调用 fortuneApi.getZodiacFortune({ sign }) → 在内容屏上显示结果/加载/错误状态。
  - 内容屏使用 offscreen Canvas + CanvasTexture，512×320 分辨率，绘制运势评分条、幸运信息、总结文字。
  - 原空白星座屏已从 BlankContentScreens 移除，由 ZodiacWheel 自带的动态内容屏替代。
- `npm run build` 通过（584 modules，tsc 无错误，Vite 5.38s）。
- 用户反馈：不要悬浮球体和文字，把高亮与交互做在轮盘圆盘上。改为 12 个 RingGeometry 扇区（内径 0.45、外径 1.65），覆盖在 zodiac-wheel 模型上方。
- 内容屏从 2.45x1.55 放大到 3.6x2.25，Canvas 分辨率 512x320 → 768x480，绘图改为相对布局。
- `fortuneApi.ts` 修复：mock 模式下 getZodiacFortune 从 zodiac_texts.json 按星座动态取数据，不再是全白羊座。

## Layer 8 — 塔罗桌 3D 交互组件
- 新建 `TarotTable.tsx`（~380 行），实现完整 4 步流程：
  1. IDLE：水晶球位置 [0, 1.42, 3.72]，raycasting 命中时高亮金色辉光，左键点击进入输入。
  2. QUESTION：HTML overlay 对话框（fixed 定位、半透明遮罩、居中卡片），输入"你想问什么"，确认/取消退出 Pointer Lock 后恢复。
  3. SELECT：22 张大阿卡纳以弧形排列（中心 [0,1.55,1.5]，半径 3m，-65°~+65°）。Card plane（0.35×0.55）显示牌背 Canvas 纹理。Raycasting 命中高亮金色描边，左键选中/取消，复用 tarotLogic 全部函数。显示"过去/现在/未来"进度灯。
  4. REVEAL：选满 3 张 500ms 后自动调 getTarotReading()。选中牌 scaleX 翻面动画（cos 插值），牌面纹理替换。塔罗内容屏 Canvas 纹理展示三列结果（牌名/正逆位/关键词/解读）+ 总结。
- 空白塔罗屏已从 BlankContentScreens 移除。
- `npm run build` 通过（588 modules，tsc 无错误，Vite 7.39s）。
