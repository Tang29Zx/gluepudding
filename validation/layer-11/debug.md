# Layer 11 调试记录

本文件只记录 Layer 11：真实门禁控制层 的问题、原因判断、解决方案和验证结果。不要记录 secret、token、私钥、密码或内部敏感地址。

记录格式：

```text
日期：
版本 / Layer：
现象：
原因判断：
解决方案：
涉及文件：
验证结果：
画面变化：
截图：
剩余风险：
```

日期：2026-07-09
版本 / Layer：Layer 11 真实门禁控制层搁置
现象：用户要求将 Layer 11 标注为搁置。
原因判断：当前已接入的登录态只作为实验室传送权限的世界交互门槛；真实门禁控制需要额外服务端授权、IoT 接口、安全确认和二次确认流程，不作为当前阻塞项推进。
解决方案：在 `VALIDATION_LAYERS.md` 的 Layer 11 段落增加“状态：搁置”和搁置说明，并在 `MEMORY.md` 记录 Layer 11 不再作为当前验收阻塞项；同时调整 Layer 12 前置条件，避免继续要求 Layer 11 先通过。
涉及文件：`VALIDATION_LAYERS.md`、`MEMORY.md`、`validation/layer-11/debug.md`
验证结果：本次为文档状态归档，不修改运行时代码。
画面变化：否。
截图：无，本次只更新验收记录。
剩余风险：后续恢复 Layer 11 时，需要重新确认门禁状态接口、开门接口、角色权限、生产代理路径和敏感错误处理。
